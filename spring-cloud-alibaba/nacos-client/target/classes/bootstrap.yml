spring:
  application:
    name: nacos-client
  profiles:
    active: dev  #开发环境，这里为dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml #配置格式
        refresh-enabled: true
        namespace: public